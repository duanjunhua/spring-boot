<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kingroad.pulsar.mapper.SysPermissionMapper">

    <select id="selectPermsByRoleCodes" resultType="java.lang.String">
        SELECT DISTINCT m.permission_code
        FROM t_permission m
        INNER JOIN t_role_permission rm ON m.id = rm.menu_id
        INNER JOIN t_role r ON rm.role_id = r.id
        WHERE 1=1
        <if test = "roleCodes != null and roleCodes.size() > 0">
            and r.role_code IN
            <foreach collection="roleCodes" item="code" open="(" separator="," close=")">
                #{code}
            </foreach>
        </if>
        AND m.permission_code IS NOT NULL AND m.permission_code != ''
    </select>

    <select id="selectAllMenuTree" resultType="com.kingroad.pulsar.entity.mr.SysPermission">
        SELECT id, parent_id, permission_name, resource_path, permission_code, resource_type, sort_order, enable
        FROM sys_menu
        ORDER BY parent_id ASC, sort ASC
    </select>
</mapper>